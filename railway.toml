[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "always"
restartPolicyMaxRetries = 5

# Railway Volume: monta /data como disco persistente
# Se configura en el dashboard de Railway, no aquí
